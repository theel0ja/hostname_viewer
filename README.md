# hostname_viewer

## Setup
```bash
virtualenv venv
source venv/bin/activate
pip install netaddr
pip install Flask
```

## Run
```bash
sudo -u www-data uwsgi --ini uwsgi.ini
```

## Dependencies
```bash
pip install netaddr
pip install Flask
```
